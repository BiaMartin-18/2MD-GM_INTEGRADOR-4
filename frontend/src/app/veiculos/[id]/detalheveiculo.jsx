"use client"

import { useEfect, useState } from "react";
import "./veiculo.css"

export default function DetalhesVeiculos({ vehicleId }) {
    const [veiculo, setVeiculo] = useState(null)

    useEffect(() => {
        if (vehicleId) {
            
        }
    })
}